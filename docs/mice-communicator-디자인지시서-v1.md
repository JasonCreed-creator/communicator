# MICE 커뮤니케이터 — 디자인 업그레이드 지시서 v1 (전체 depth)

| 항목 | 내용 |
|---|---|
| 목적 | Tailwind 기본 회색 체계 → 리멤버 웜 페이퍼 룩 전면 전환 + 화면 위계·구분감 확립 |
| 근거 | 2026-08-22 진단: public/brand 로고 부재, 브랜드 토큰 0건, 전 화면 gray-* 단일 톤 |
| 범위 | S0~S9 + 발주처 뷰 전체. 기능·데이터 로직 무변경(스타일·레이아웃만) |
| 첨부 | communicator-brand-assets.zip (로고 2종, public/ 구조 그대로) |

---

## 1. 디자인 토큰 (정본 — `src/styles/tokens.css`에 CSS 변수로 등록 후 전 화면 치환)

| 토큰 | 값 | 용도 |
|---|---|---|
| --canvas | #FBFAF6 | 앱 배경 (웜 아이보리) |
| --card | #FFFFFF | 카드·시트 |
| --border | #DCD6C8 | 1px 웜 보더 (기본 구분선) |
| --track | #EFEBE0 | 진행바 트랙·비활성 면 |
| --ink | #1A1A1A | 본문 (퓨어 블랙 #000 금지) |
| --ink-sub | #6E6E6E | 보조 텍스트 |
| --ink-cap | #756F64 (v1.4 — 옛 #8C867A) | 캡션·라벨 — 흰 바탕 4.98:1 · canvas 4.77:1 (§7-2.1) |
| --brown | #4A463F | 서브 잉크·섹션 번호 |
| --accent | #EB6F2A | 리멤버 오렌지 — CTA·활성·강조 |
| --accent-deep | #B8431A | 액센트 텍스트·호버 |
| --accent-tint | #FFF1E6 | 액센트 배경 틴트 |
| --steel | #476580 | 보조 컬러 (정보성) |
| --dark | #211E1A | 사이드바·다크 바 |
| --dark-ink | #F4F0E9 | 다크 위 텍스트 |
| --positive | #196B24 / 틴트 #E8F2E9 | 승인·완료 |
| --negative | #C42F2F (v1.4 — 옛 #D93636) / 틴트 #FBEAEA | 수정요청·지연 — 틴트 위 배지 글자 4.76:1 (§7-2.1) |
| 그림자 | 0 1px 3px rgba(74,70,63,.08) 한 단계만 | 카드 |
| 라운드 | 버튼 6 / 카드 10~12 / 뱃지 pill | — |
| 간격 | base 4px 배수 | — |

**금지**: gray-*·slate-* 등 쿨 그레이 전면 제거(치환 후 `grep`으로 0건 증명), #000, 그라디언트(홈 히어로 1곳 외 금지), 보라·파랑 계열 임의 사용(steel 제외).

## 2. 타이포그래피

- Pretendard(CDN dynamic-subset) 전역 적용, 숫자는 `font-variant-numeric: tabular-nums`
- 스케일: 페이지 타이틀 25/600 · 섹션 타이틀 20/600 · 카드 타이틀 16/600 · 본문 14/400 · 캡션 12/500(+letter-spacing .02em, --ink-cap)
- KPI 숫자 31/650

## 3. 상태·역할 컬러 체계 (전 화면 통일)

| 상태 | 뱃지 (틴트 bg / 텍스트) |
|---|---|
| 지시됨 requested | --accent-tint / --accent-deep |
| 작성중 draft | --track / --ink-sub |
| 내부검토 internal_review | #EAF0F5 / --steel |
| 컨펌요청 pending_approval | --accent-tint / --accent-deep + 좌측 도트 |
| 수정요청 changes_requested | negative 틴트 / --negative |
| 승인·확정 approved·final | positive 틴트 / --positive |
| 지연 | --negative | 임박 | --accent |

간트·담당 역할 컬러: pm #4A463F · design #EB6F2A · ops #476580 · reg #F3B48A — R&R 카드 좌측 보더에도 동일 적용.

## 4. 레이아웃 전면 전환 — 구분감의 근본 해결

- **내부 화면: 상단 탭 → 좌측 사이드바(고정 232px, --dark 배경)**. 상단에 offwhite 로고, 메뉴는 아이콘+라벨(--dark-ink 70%), 활성 항목 = 오렌지 3px 좌측 마커 + 텍스트 100%. 하단에 행사명·유형 뱃지·설정. 모바일(<768px)은 햄버거 드로어.
- 콘텐츠 영역: --canvas 배경, max-width 1120 중앙, 페이지 헤더 패턴 통일(캡션 라벨 → 타이틀 25 → 우측 액션 버튼).
- **발주처 뷰: 사이드바 없음.** 슬림 다크 상단 바(--dark + offwhite 로고 + 행사명), 이하 모바일 1열 카드.

## 5. 컴포넌트 규격

- 카드: --card + 1px --border + r12 + 그림자 1단계. 카드 안 카드 금지(면 분리는 --canvas 인셋으로).
- 버튼: primary(--ink bg/white) · accent(--accent bg/white — 컨펌 발송·승인 등 핵심 CTA 전용, 화면당 1개 원칙) · ghost(1px --border). 높이 36, r6.
- 테이블: 헤더 = 캡션 스타일(--ink-cap, 12/500) + 하단 1px --border-strong, 행 높이 44, hover --accent-tint 30%, 줄무늬 금지.
- 입력: --card bg + --border, focus 시 --accent 1.5px 링. 라벨은 캡션 스타일. **상세 규격은 §5-1 입력 폼 정본.**
- 진행바: 트랙 --track, 필 --accent, 높이 6, r3. 수치 라벨은 항상 바 **아래** 줄(겹침 재발 금지).
- 빈 상태: 중앙 아이콘(--ink-cap) + 한 줄 설명 + ghost CTA.

## 5-1. 입력 폼 정본 (v1.2 — 핸드오프 `패턴 기준 시트` §10 · `폼 정본 폴리싱` 2026-08-29 채택)

> **왜 뒤늦게 생겼나**: v1과 패턴 기준 시트 01–09가 입력 폼을 규정하지 않았다. 그래서 컨트롤 81개가
> 브라우저 기본값 위에 서 있었다 — 체크·라디오 19개가 브라우저 파랑으로 렌더돼 §1의 "파랑·보라 계열
> 임의 사용 금지"를 네이티브 컨트롤 쪽에서 우회했고, 숫자·날짜·셀렉트 62개는 정렬·자릿 구분·화살표가
> 화면마다 달랐다. 실장 위치는 `src/index.css`(CSS)와 `src/components/internal/`(컴포넌트)이다.

### 5-1.1 컨트롤 6종 — 높이 36 · r6 · 포커스 accent 1.5px (`.ui-input` 확장)

| 종류 | 클래스 | 규격 |
| --- | --- | --- |
| 텍스트 | `.ui-input` | 기본 — 그대로 |
| 수량·정원 | `.ui-input ui-input-num` | 우측정렬 · tabular-nums · **스피너 제거** |
| 금액 | `MoneyField` | 천단위 구분 + 아래 줄 한글 에코(`1,200만원`, --accent-deep) |
| 날짜·시간 | `.ui-input` | **네이티브 유지** · 표기 `YYYY-MM-DD` 고정 (`ui-input-num` 금지) |
| 셀렉트 | `.ui-input ui-select` | OS 화살표 제거 → ink-cap 셰브론 자체 렌더 · 우측 여백 32 |
| 긴 텍스트 | `.ui-input` + `rows={3}` | 3줄 고정 · 자동 확장 없음 |

`accent-color: var(--accent)`는 `@layer base`에 **1회만** 선언한다 — 이 한 줄이 컨트롤 전부를 정본에
넣는다. 화면이 인라인으로 되칠하지 않는다(상시 가드).

### 5-1.2 체크 · 라디오

- 크기 **16(내부) · 20(외부 지면)** — `.ui-check` / `.ui-check ui-check-lg`
- 클릭 영역은 컨트롤이 아니라 **라벨 행 전체** — 감싸는 라벨에 `.ui-check-row`(min-height 36)
- `/c`·`/p`·현장 체크인은 `.ui-check-row-touch`로 **행 높이 44 고정** — §7-1.7 터치 규정은 버튼만의 규정이 아니다

### 5-1.3 선택 카드 — 표시는 두 겹까지

선택 표시는 **`border-accent` + `bg-accent-tint`**까지다. `선택` 필(pill)을 얹지 않는다 — 라디오·보더·
틴트가 이미 세 번 말한다. 우측 배지 자리는 `가정`처럼 **정보를 더하는 배지 전용**이다(accent 필과
나란히 서면 둘 다 안 읽힌다).

### 5-1.4 필드 상태 5종 (`Field` — `src/components/internal/Field.tsx`)

기본(힌트 한 줄) · 포커스(accent 1.5px 링) · 오류(`.ui-input-error`) · 비활성(`.ui-input:disabled` —
track 면 + ink-cap) · 읽기 전용(`.ui-input-readonly` — 면·보더 없이 값만 600).

- **오류는 힌트 줄을 대체한다. 두 줄로 쌓지 않는다** — 필드 높이가 폼마다 달라진다.
- 필드 단위 오류가 있으면 `ErrorAlert` 블록을 겹쳐 띄우지 않는다. **블록 경고는 저장 실패(서버·권한) 전용.**
- 필수는 라벨 뒤 `*` 하나(--accent-deep). `required` 배지·빨간 라벨은 쓰지 않는다.

### 5-1.5 폼 안 버튼 위계 — 화면당 주 버튼 1개

- 행·카드 단위 저장은 `btn btn-ghost btn-sm`, 라벨은 **`저장`** — 카드 제목을 버튼이 다시 읽지 않는다.
- 변경 전에는 **비활성**, 저장 후에는 버튼 옆 `저장됨 hh:mm` 캡션. 성공 배지·토스트는 쓰지 않는다.
- `btn-accent`는 외부 지면 승인 CTA 전용 — **내부 폼 저장에 쓰지 않는다.** 위저드의 전진 버튼만 주 버튼이다.

### 5-1.6 인쇄

`.ui-select`는 셰브론과 우측 여백을 되돌리고, `.ui-input`·`.ui-input-readonly`는 배경을 비우고 보더만
남긴다 — 종이에서 누를 수 없는 컨트롤이 잉크를 먹지 않게 한다.

## 6. 화면별 depth 지시

- **S0 온보딩**: 캔버스 중앙 카드(maxw 720). 좌측 세로 스텝 레일(완료=오렌지 체크). 유형 카드 선택 시 오렌지 2px 보더 + --accent-tint.
- **S1 홈**: 상단 KPI 4카드(미결 컨펌·지연 태스크·임박·D-day — 숫자 31 tabular). 아래 2열: 좌 '내 할 일'(지시·컨펌 대기 통합 리스트) / 우 최근 활동·인박스. 지연 항목은 negative 도트.
- **S2 보드**: 카테고리 그룹 헤더(브라운 캡션+건수). 항목 카드 좌측에 상태 컬러 스트립 3px — 목록만 봐도 상태 분포가 보이게.
- **S3 상세**: 2단 분할 — 좌(660) 지시 카드(--accent-tint 배경)·미리보기·코멘트, 우(300) 메타 사이드(상태·담당·마감·버전 타임라인 세로선·Drive 링크). 큐시트 항목은 에디터가 좌측 전폭.
- **S4 등록**: 상단 통계 3카드, 탭은 하단 오렌지 언더라인 방식, 테이블 규격 §5 적용.
- **S5 WBS**: 단계 필터 pill(활성=--dark bg/white). 간트 바 = 역할 컬러(§3), 오늘 = 오렌지 세로선, 완료 바는 40% 투명. R&R 카드 좌보더 = 역할 컬러.
- **S6 설정**: 섹션별 카드 분리(멤버/발주처 토큰/유형/연동), 위험 동작(토큰 회수)은 negative ghost.
- **S9 운영계획서**: **종이 메타포** — 캔버스 위 중앙 white 시트(maxw 880, 상단 오렌지 4px 헤어라인). 섹션 번호는 브라운 대형 **01~08**(정본 = `src/components/plan/planSections.ts`의 `PLAN_SECTION_ORDER` — 01 행사개요 · 02 프로그램 · 03 큐시트 · 04 존별 운영 · 05 제작물 리스트 · 06 등록 통계 · 07 비상 대응 · 08 일정), 섹션 진행률은 우측 캡션. 인쇄 CSS는 시트만 출력.
- **발주처 큐·현황**: 모바일 우선 1열. [승인] = accent 대형 버튼, [수정요청] = ghost. 확정본은 positive 뱃지. 375px에서 손가락 타깃 44px 이상.

## 7. 로고 적용

첨부 ZIP → `public/brand/` 배치. 사이드바·다크 바 = `remember-logo-offwhite.png`, 라이트 컨텍스트·인쇄 = `remember-logo-black.png`. 높이 20~24px로만 스케일(비율 왜곡·재염색 금지). 텍스트 폴백은 이미지 로드 실패 시에만.

## 7-1. 공통 패턴 정본 (v1.1 — 디자인 핸드오프 `패턴 기준 시트` 2026-08-28 사용자 승인)

> v1이 "미정의"로 남긴 **표·빈 상태·차트·인쇄 규격**을 정본화하고, 5계열로 흩어져 있던 **상태 배지를 의미 4단계로 통합**한다.
> 본 절과 §3이 충돌하면 **본 절이 우선**한다. 구현체는 `src/index.css`(표·인쇄) · `src/lib/labels.ts`(레벨 매핑) ·
> `src/components/internal/*`(프리미티브)이며, 화면은 이들을 **재구현하지 말고 사용**한다.

### 7-1.1 상태 배지 — 의미 4단계 + 중립

색은 **의미**만 나타낸다. 계열(컨펌·WBS·파트너·정산·시트)은 색이 아니라 **배지 앞 접두 라벨**로 구분한다.

| 단계 | 배경 / 텍스트 | 뜻 |
| --- | --- | --- |
| 중립 `neutral` | `track` / `ink-sub` | 아직 시작 안 함 — 초안·미배정·미연결 |
| 진행 `progress` | `steel-tint` / `steel` | 움직이는 중, 개입 불필요 |
| 주의 `attention` | `accent-tint` / `accent-deep` | **내 행동을 기다림** |
| 정상 `positive` | `positive-tint` / `positive` | 끝남 — 승인·확정·완료 |
| 차단 `blocked` | `negative-tint` / `negative` | 막힘·지연 — 수정요청·초과·실패 |

5계열 매핑(중립 / 진행 / 주의 / 정상 / 차단):

| 계열 | 중립 | 진행 | 주의 | 정상 | 차단 |
| --- | --- | --- | --- | --- | --- |
| 컨펌 | 초안 | 내부검토 | 컨펌대기 ● | 승인·확정 | 수정요청 |
| WBS | 미착수 | 진행 | 마감 임박 | 완료 | 지연 |
| 파트너 | 미배정 | 참여 중 | 제출 임박 | 제출 완료 | 철회 |
| 정산 | 미정산 | 정산 중 | 확인 필요 | 정산 완료 | 예산 초과 |
| 시트 | 미연결 | 동기화 중 | 갱신 있음 ● | 연결됨 | 권한 끊김 |

**규칙** — 좌측 6px 도트는 **'내 행동을 기다리는' 상태 하나에만**(컨펌 `pending_approval`, 시트 '갱신 있음').
주의 단계 전부에 붙이면 신호가 죽는다. 한 화면에 한 계열만 나오는 표에서는 접두 라벨을 생략한다(열 헤더가 이미 계열을 말한다).
색만으로 구분하지 않으며 배지는 언제나 라벨 텍스트를 동반한다. 카드 좌측 3px 상태 스트립은 배지의 원색 계열 그대로, 중립만 `border-strong`.

**인쇄 변형** — tint 배경 제거 → 1px 보더 + 잉크 텍스트 + 선행 글리프(`○ 초안` / `◐ 내부검토` / `● 컨펌대기` / `✓ 승인` / `▲ 수정요청`).
주의·차단만 1.5px, 차단은 점선.

### 7-1.2 역할 표시 — 형태로만

상태 = **면**(tint 배경 pill). 역할 = **형태**(좌측 4px 바 · 8px 도트 · 간트 바). **역할에 pill 배지를 쓰지 않는다.**
표에서 역할 바는 스티키 첫 열 **바깥 최외곽**에 두거나 담당자 이름 앞 도트로 — 스티키 열과 겹치면 가로 스크롤 시 잘린다.

### 7-1.3 표 정본

| # | 규칙 |
| --- | --- |
| 01 | 행 높이 **44 고정**이 기본값 |
| 02 | **밀집 모드 36** 토글 병행 |
| 03 | 숫자·금액 **우측정렬** + tabular |
| 04 | 첫 열(이름) **스티키 고정** + 우측 1px 경계선 |
| 05 | 헤더 **정렬 화살표** — 비활성 `↕` / 활성 `↓ ↑` |
| 06 | 행 hover `accent-tint/30` |
| 07 | **줄바꿈 금지 + …처리** |
| 08 | **그룹 헤더행** — canvas 면 + 12/600 + 건수 |
| 09 | 합계·소계는 **고정 하단행** + `border-strong` 상단선 |
| 10 | 셀 내 **진행률 막대** 6px + 아래 줄 수치 |
| 11 | **헤더 셀(`.ui-th`)과 배지는 표 종류와 무관하게 줄바꿈 금지**(2026-09-04 실측 정정 — 운영계획서 인쇄본에서 "카테고 리"·"컨펌대 기"처럼 글자 단위로 끊김). `.ui-table` 밖의 **문서형 표**(S9 인쇄·편집기)는 07을 통째로 받지 않는 대신, 짧은 식별 칸(카테고리·수량·시간·큐·구분·최신 시안·상태)만 `whitespace-nowrap` + 전 칸 **상단 정렬**, 긴 본문 칸(품명·규격·내용)은 접힌다. 본문 칸 좌우 패딩은 **12px(`px-3`)** — `.ui-th`의 12px와 좌측 기준선을 맞춘다(패딩이 `pr-*`뿐이면 본문이 헤더보다 12px 왼쪽에서 시작한다) |

헤더 재질: `background: canvas` + 하단 **2px `border-strong`**, 짝수행 zebra `canvas`, 첫 열 600.

**적용 조건** — ① 밀집 모드는 **내부 관리 화면에만**(현장 체크인·발주처 `/c`·파트너 포털 `/p` 금지 — 터치 타깃 44 미만 불가)
② …처리에는 `InfoTip` 툴팁이 따라붙어 전체 값을 확인할 수 있어야 한다 ③ 정렬 화살표는 **정렬 가능한 열에만**(큐시트처럼 순서가 의미인 표에는 금지)
④ 셀 내 막대는 **진행률 열에만**(금액·수량 열 금지).

**인쇄 변형** — 헤더 canvas 면 제거(2px 룰만) · zebra 3% · 헤더행 페이지마다 반복 · 행 중간 분할 금지 ·
소계/합계 위 진한 괘선 · 밀집 모드는 인쇄에 미적용.

### 7-1.4 빈 상태 5종

| 층 | 언제 | 형태 | 구현체 |
| --- | --- | --- | --- |
| ① 로딩 | 0.3초 이상 | **스켈레톤** — 실제 행 구조와 같은 형태. **스피너 금지** | `TableSkeleton` |
| ② 데이터 없음 | 아직 만든 적 없음 | 아이콘 + 한 줄 + **ghost** 주 액션 1개 | `EmptyState` |
| ③ 필터 결과 없음 | 데이터는 있음 | **전체 건수 + 적용된 필터 칩 + 초기화** — ②와 반드시 구분 | `FilterEmptyState` |
| ④ 권한 없음 | 역할 게이트 | steel 배너로 **이유 + 요청 경로**. 메뉴에서 지우지 않음 | `PermissionNotice` |
| ⑤ 로드 실패 | 조회 오류 | `ErrorAlert` 원문 그대로 + **재시도** + 직전 스냅숏(기준 시각 명기) | `LoadFailedState` |

**②에 accent를 쓰지 않는다** — 빈 화면의 유일한 CTA가 accent를 독점하면 화면당 1개 원칙이 무너진다.

### 7-1.5 시각화 어휘 (수위: 중간)

| 요소 | 규격 | 구현체 |
| --- | --- | --- |
| 진행률 바 | 6px · r3 · `track`/`accent`. 수치는 **항상 바 아래 줄 우측**. 100%는 `positive` | `ProgressBar` |
| 스택 막대 | 12px · r999 · accent 3단(`accent-deep` → `accent` → `role-reg`). 구성 비율 전용, **3단 초과 금지**(4번째는 중립 잔여 구간) | `StackedBar` |
| 도넛 | 지름 96 · 두께 12. **단일 비율 1개만**, 다항목 비교 금지 | `Donut` |
| D-day 스트립 | 168px 카드 · 지난/이번/다가올 3단. **`overflow-x:auto`**(클립 금지) | `PartnerDeadlineTimeline` |
| KPI 카드 | 숫자 31/650 + 캡션 + **보조 수치 1줄**(구분선 위) | `StatTile` (`support`) |
| 간트 바 | 20px · r4 · 역할 컬러. 완료 = 역할색 40% 투명, 임박 = accent, 지연 = negative(역할색 덮어씀) | `WbsGantt` |

### 7-1.6 인쇄 규격

**2종(화면·인쇄) 규정 대상은 4가지만** — 표 · 배지 · KPI 카드 · 섹션 헤더.
나머지는 일괄 **"인쇄 시 숨김"**(툴팁 · 드롭다운 · 모달 · 관리용 요약 패널 · 편집/인쇄 버튼 · 필터 바 · 페이지네이션 · 스켈레톤).
전역 유틸리티는 `.print-hidden`이며 기존 `.plan-print-hidden`은 별칭으로 유지한다.

### 7-1.7 외부 지면 (`/c`, `/p`)

**같은 토큰, 다른 레이아웃 원칙** — 외부는 카드 우선, 내부는 표 우선.
배지 어휘는 내부와 동일하다(승인 대기·승인됨·수정요청은 상태 머신의 실제 값이라 축약 시 정보 손실).
터치 타깃 **44 고정**(`btn-sm` 28은 외부 금지) · 밀집 모드 금지 · **1열 스택**(데스크톱에서도).
역할 컬러·간트·내부 KPI처럼 운영 맥락이 필요한 시각화는 내보내지 않는다.

### 7-1.8 인물 카드 · 역할 칸 — 담당자 배정 (v1.3 · Phase 3.22, 2026-09-24 사용자 지시)

"미리 배치하지 말고 인물카드를 만들어서 클릭하거나 드래그앤드랍으로" — 온보딩 ②·행사 설정 ② 공용(`MembersEditor`).

| 요소 | 규격 |
|---|---|
| 역할 칸 4개 | PM·디자인·운영·등록 순. `bg-canvas` · r8 · 패딩 10 · 최소 높이 120. 열 수는 **놓인 칸의 폭** 기준(컨테이너 쿼리 — 행사 설정 ②는 좁은 열, 온보딩 ②는 넓은 본문): 24rem↑ 2열 · 48rem↑ 4열 |
| 칸 머리 | **8px 도트 + 역할명**(§7-1.2 — 역할에 pill 배지 금지) · 우측 `n명` 캡션 |
| 배정 카드(칸 안) | `bg-card` · 1px `--border` + **좌측 4px 역할 바** · 이름(14 medium)·직함·이메일·전화 — 넘치면 말줄임 + 마우스 올리면 전체 값 · 우측 `빼기`(negative 밑줄 텍스트) |
| 빈 칸 | 점선 1px 상자 + "카드를 여기로 끌어놓기"(읽기 전용은 "배정 없음") |
| 주소록 카드(후보) | `bg-card` · 1px `--border` · r8 · `shadow-card`. 머리 = 32px 원형 이니셜(`--track` 면 · `--brown` 글자) + 이름·직함. 놓인 칸 폭 기준 24rem↑ 2열 · 42rem↑ 3열 |
| 누르기 | 카드 테두리 `--accent` + 역할 버튼 4개(`btn-ghost btn-sm`, 버튼 안 역할 도트). 다시 누르면 닫힌다 |
| 끌기 중 | 모든 칸 점선 2px `--border-strong` → 올린 칸만 `--accent` 테두리 + `--accent-tint` 면 + "여기에 놓으면 배정됩니다" |
| 받지 않는 끌기 | 파일·글자 등 사람 카드가 아닌 끌기는 칸이 반응하지 않는다(강조 없음) |
| 처리 중 | 카드 `opacity-60` · 끌기·누르기 잠김 |

- 배정된 카드는 끌 수 없다 — 역할 변경은 `빼기` 후 다시 배정(두 동작이 각각 보인다).
- 끌기를 못 쓰는 터치·키보드는 누르기로 같은 일을 한다(카드 = `role="button"` · Enter/Space).
- 새 색·새 의존성 없음 — 토큰은 전부 §1, 끌기는 브라우저 기본(HTML5 DnD).

## 7-2. UX 개편 (v1.4 — Phase 3.23, 2026-09-25 캔버스 v4 승인)

근거: 2026-09-25 UX 진단(공통 결함 8건)과 개선 시안 캔버스 "커뮤니케이터 UX 개편"(v4 — 3턴 렌더 검수 반영). 화면별 규격은 구현 PR마다 이 절에 이어 쓴다(PR-1 = 기반).

### 7-2.1 글자 대비 — 본문 크기 4.5:1 (사용자 결정 2026-09-25)

| 대상 | 전 | 후 | 대비 |
|---|---|---|---|
| `--ink-cap` (캡션·표 머리·라벨) | #8C867A (3.62:1) | **#756F64** | card 4.98 · canvas 4.77 |
| `--negative` (지연·수정요청·음수) | #D93636 (틴트 위 3.99:1) | **#C42F2F** | negative-tint 4.76 · card 5.53 |
| `.btn-accent` 바탕 (흰 글자) | `--accent` (3.07:1) | **`--accent-deep`** | 5.45 |

- **흰 글자를 올리는 주황 면은 `--accent-deep`** — 버튼·"현재" 배지·단계 표시·언어 토글 선택 상태. `--accent`는 선·점·진행바·스트립처럼 글자가 없는 면에만 쓴다.
- `.btn-accent` 호버는 새 토큰 없이 `color-mix(in srgb, var(--accent-deep) 82%, var(--ink))`.
- 예외: 외부 지면 `/p` 헤더 배지(이번 개편 범위 밖 — 사용자 선택).
- 가드: `src/test/dod70-ux-foundation.test.tsx` ①·②.

### 7-2.2 날짜 표기 — 남은 날 `D-n` · 지난 날 `n일 지남`

| 함수 (`lib/labels.ts`) | 미래 | 당일 | 지남 | 쓰는 곳 |
|---|---|---|---|---|
| `dueLabel` | D-7 | **오늘** | **3일 지남** | 내부 화면의 기한(항목·WBS·마일스톤·파트너 마감) |
| `eventDayLabel` | D-27 | D-day | **15일 지남** | 내부 화면의 행사일(사이드바 셀렉터·홈·행사 목록) |
| `ddayLabel` | D-7 | D-day | D+3 | **발주처 화면·운영계획서 문서**(관행 표기 유지 — 외부 지면은 이번 범위 밖) |

- 지난 기한을 `D+3`으로 쓰면 남은 날 `D-3`과 한 글자 차이라 헷갈린다(진단 4). 빨강은 지난 기한에만.
- **끝난 일에는 'n일 지남'을 붙이지 않는다** — 완료 태스크·완료 마일스톤의 기한 칸은 `—`(상태 배지가 이미 '완료'), 확정 항목은 '완료'.
- WBS 템플릿 오프셋(`D-42~D+30`)은 **행사일 기준 상대 위치**라 기한이 아니다 — `offsetLabel` 그대로.

### 7-2.3 화면 코드 비노출

- 페이지 머리 캡션은 사이드바 그룹 이름(`운영`·`준비`·`전체`)만 — `S1`·`S-10` 같은 코드는 개발 문서 전용이다(진단 3).
- 항목 상세 경로는 `보드 › 카테고리`(끝의 `S3` 제거) · 견적 목록 캡션 `준비`(EN `PREP`) · 런처 카드 칩은 쓰임새(`견적 단계` · `행사 준비 · 운영 · 정산`).
- 안내 문구 안의 코드도 이름으로: `등록(S4)으로 유입` → `등록 보드로 유입` · `S9 조립` → `운영계획서 조립`.
- 가드: `dod70` ⑤(페이지 `caption=`·견적 캡션 스캔).

### 7-2.4 한글 줄바꿈

- `html { word-break: keep-all; overflow-wrap: break-word; }` — 한글은 단어 단위로 끊는다("보 도"·"오릅 니다" 제거). 띄어쓰기 없는 긴 문자열(URL·파일 이름)은 칸 안에서 끊긴다.
- 표의 nowrap 규칙(§7-1.3 규칙 11)은 그대로 — 이 설정은 줄바꿈이 **일어나는 곳**의 끊는 위치만 바꾼다.

### 7-2.5 홈 — 요약 5칸 + '오늘 할 일' 한 목록 (PR-2)

진단 1 "다음 할 일이 흩어져 있다" → 사용자 결정 "'오늘 할 일' 목록 하나로 통합". 3.17의 3분할 큐·D-day 스트립·정산 경보 띠·인박스·받은 가이드·마일스톤 카드를 대체한다.

| 영역 | 규격 |
|---|---|
| 머리 | 캡션 `운영` · 제목 `홈 대시보드` · 우측 = 행사명(14/600) + 캡션(요일 날짜 · 장소 · 확정 x/y) + 행사 D-day dark pill(높이 44 · 20/600 — `eventDayLabel`) |
| 요약 5칸 | 지연(0보다 크면 `--negative`) · 마감 임박(3일 안) · 발주처 답 대기(**주최형 = 파트너 검토 대기**) · 정산 확인(견적 초과 버킷 수 — 금액 없음) · 미등록 파일. 카드 · 캡션 12 + 숫자 24/700 + 설명 12 한 줄. 나머지 칸 숫자는 잉크 |
| 오늘 할 일 | 출처 8종을 **급한 순 한 표**: 지연 태스크·지난 마일스톤 → 컨펌대기·파트너 검토 → 정산 초과 → 미등록 파일 → 받은 가이드 → 임박(같은 급은 기한 이른 순). 표 정본 5열(상태 배지 · 할 일(코드·제목 + 한 줄 설명) · 담당(역할 도트 + 이름, 모르면 `역할 담당`) · 마감(날짜 + `dueLabel`, 지난 기한만 빨강) · 바로 하기 1개(ghost-sm)). 도트는 발주처·파트너 답을 기다리는 행에만. **일정 행(지연·지난 마일스톤·임박)은 8행까지** 보이고 나머지는 `일정 행 n건 더 보기` — 넘겨받은 일(컨펌대기·파트너 검토·정산·미등록 파일·받은 가이드)은 **항상 보인다**(전체 행 수로 자르면 지연 수십 건 뒤로 밀려 가려진다) |
| 칩 | `전체 · 내 차례 · 지연 · 컨펌·정산`(주최형 `검토·정산`) — 건수 굵게, 눌린 칩 = ink 면 흰 글자. `지연` = 기한 지난 모든 행. 걸러서 0건이면 필터 빈 상태(전체 건수·적용 필터·초기화 — §7-1.4 ③) |
| 내 차례 | WBS 태스크·영역 마일스톤 = 그 역할 · 컨펌대기·파트너 검토·정산·미등록 파일·전체 마일스톤 = pm · 받은 가이드 = 늘 내 것 |
| 일괄 동작 줄 | 칩 아래 canvas 띠: `담당에게 리마인드`(지연 전부) · `컨펌 독촉`(대행형 — 컨펌 대기 전부) · `Drive 지금 확인`(실서버 + 연결됨) — 전부 ghost. 결과·mock 안내는 띠 아래 `role="status"` 한 곳. 서버 알림이 목록 단위라 행마다 달지 않는다 |
| 미등록 파일 | 바로 하기 = `항목에 연결` → 행 아래 펼침(항목 선택 · 연결(primary sm) · 무시). 처리하면 행과 요약 칸이 즉시 준다 |
| 아래 줄 | 다가오는 2주(2/3 폭 — 오늘~14일 미완료 일정·마일스톤, `M월 D일 (요일)` · ◆ 표지(마일스톤·파트너 제출 = accent, 주최 통지 = steel, 내부 = border-strong) · D-n, 8줄까지) · 영역별 확정 · 최근 활동(5건) |

- 채운 버튼 0개 — 홈은 훑고 넘어가는 화면이다(펼친 인박스 패널의 `연결`만 예외).
- 금액 0건 — 정산 행은 버킷 이름만 싣고 `정산보드에서 보기`로 보낸다(§19.7 · DoD 30과 같은 결).
- 가드: `src/test/dod71-home-today.test.tsx` · `align-home.test.tsx`.

### 7-2.6 디자인 보드 — '다음 행동' 표 · 차례 칩 · 갤러리 (PR-3)

진단 2 "상태만 보이고 다음에 무엇을 할지가 없다" → 캔버스 디자인 보드(목록·갤러리 2장) + 사용자 결정 "보드에 갤러리 보기 추가". 3.17b의 카테고리 묶음 + 행 그리드(92·flex·48·84·132·96)를 대체한다. **운영 보드는 v2.5 유형 우선 구조(유형 카드 + 인라인 빌더)를 그대로 둔다** — 이번 범위는 디자인 영역만.

| 영역 | 규격 |
|---|---|
| 머리 | 캡션 `운영` · 제목 `디자인 보드` · 우측 ⓘ + **채운 버튼 `＋ 항목 추가` 1개**(btn-accent). 누르면 폼이 머리 아래에서 열리고 머리 버튼은 물러난다(폼의 제출 버튼이 넘겨받음 — 화면의 채운 버튼은 늘 1개). 쓰기 권한이 없거나 종료 행사면 버튼 대신 한 줄 안내 |
| 거르기 줄 | 좌: 차례 칩 `전체 · 우리 차례 · 발주처 차례 · 끝남`(주최형 = `파트너 차례`) — 구분선 — `● 지연만`(빨간 점, 켜고 끄는 칩 — 차례와 함께 걸린다). 건수 굵게, 눌린 칩 = ink 면(§7-2.5 칩과 같은 `FilterChip`). 우: 보기 토글 `목록 · 갤러리`(track 면 · 고른 쪽 card 면 600 — `SegmentedToggle`) · 담당 셀렉트(`담당: 전체`) · 제목 검색(돋보기, `제목으로 찾기`). 옛 상태 셀렉트는 칩으로 대체 |
| 표 (목록) | 카드 머리 `항목 n개`(걸렀으면 `· 전체 m개`) + 진행 막대 120 + `확정 x/m` + 우측 캡션 `급한 순 — 늦은 것, 우리 차례가 위로`. 표 정본 6열 고정 폭(`table-fixed`): **상태 112 · 항목(나머지) · 버전 56 · 담당 100 · 마감 220 · 다음 행동 300**. 행 높이 64 · 첫 칸 좌측 3px 상태 스트립 · 행 어디를 눌러도 항목 상세(키보드는 제목 링크) |
| 항목 칸 | 제목 14/600(…·title) + 한 줄 `카테고리 · 규격 · n개`(규격이 비면 `규격 미입력` — 제작물 리스트의 빈 칸을 드러낸다). 한 줄 전체를 끝에서 한 번 자른다 |
| 마감 칸 | `M월 D일 (요일)` + `dueLabel` 알약(지난 기한만 빨강). 끝난 항목은 알약 대신 `완료`, 기한 없음 `마감 미정` |
| 다음 행동 | 문구 1줄(13px · 할 일 = brown, 기다림·끝남 = ink-sub) + **버튼 최대 1개(ghost-sm)** — 뜻은 항목 상세 '다음 단계'와 같다(아래 표) |
| 급한 순 | 늦은 것(가장 오래 늦은 것부터) → 우리 차례 → 상대 차례 → 끝남, 같은 급은 기한 이른 순(기한 없음 뒤) |
| 갤러리 | 카드 3열(xl) · 2열(sm) · 1열. 최신 시안 16:9 썸네일(이미지 = `object-contain` 전체 보이기 · PDF·기타 = 확장자 표지 · 버전 없음 = 점선 자리 `아직 시안 없음 — 첫 시안 올리기` + 규격·위치, 올릴 수 있으면 업로드 카드로 가는 링크). 좌상단 상태 배지는 흰 받침 위(중립 배지가 썸네일 면에 묻히지 않게). 본문: 제목 15/600 + `vN · M월 D일` / 카테고리 · 담당 / 마감 / 구분선 아래 다음 행동(한 줄 자름). 보기 선택은 이 브라우저에 기억(`communicator.designBoardView`) |
| 아래 | 상태 범례(§7-1 단계 묶음) — 표·갤러리 아래로 내렸다 |

| 상태 | 차례 | 다음 행동 문구 | 버튼 |
|---|---|---|---|
| 가이드됨 | 우리 | 첫 시안 올리기 | `올리기`(쓰기 권한) |
| 초안(버전 없음) | 우리 | 시안 올리고 내부검토 요청 | `올리기` |
| 초안(버전 있음) | 우리 | vN 올림 — PM에게 넘기기 (권한 없으면 `담당자가 다듬는 중`) | `내부검토 요청`(기존 전이) |
| 내부검토 | 우리 | PM: 검토 후 발주처로 보내기(주최형 `검토 후 내부에서 확정`) · 그 외: PM 검토 기다림 | PM `검토하기` → 상세 |
| 컨펌대기 | 발주처 | 발주처 답 기다림 · 보낸 지 n일(당일 `오늘 보냄`) | 대행형 PM `발주처 링크` |
| 수정요청 | 우리 | 수정 요청 반영 — 새 버전 올리기 | `올리기` |
| 승인 | 끝남 | 승인됨 — 최종본으로 정리 중 | 없음 |
| 확정 | 끝남 | 끝 — 최종본 확정됨 | `최종본 받기`(버전 있을 때) |
| 파트너 항목 | 반대 | 제출·재제출 = 파트너 제출(재제출) 기다림 · 검토중 = 파트너 제출물 검토 | 검토중만 `검토하기` → 파트너 보드 |

- **보드에서 바로 올리지 않는다** — `올리기`는 `/items/{id}?upload=1`로 가고 상세가 업로드 카드로 초점을 옮긴 뒤 주소 표시를 지운다. 업로드 잠금 안내·Drive 미연결 경고·진행률·여러 파일은 업로드 카드 한 곳에만 있다(캔버스의 '끌어다 놓아 올리기'를 이 방식으로 바꾼 이탈 — 결정 로그).
- **발주처 링크**: 발주처 이메일은 아직 가지 않는다(Phase 6b) — 답을 재촉하려면 링크를 직접 다시 전한다. 살아 있는 링크가 1개면 그 자리에서 `링크 복사`(→ `복사됨`), 여럿·없음이면 `발주처 링크` → 행사 설정 ② 담당자(어느 링크인지 추측으로 고르지 않는다). 복사가 막히면 설정으로 가는 안내. 캔버스의 '다시 알리기'는 보낼 수단이 없어 이 방식으로 바꿨다(결정 로그).
- 가드: `src/test/dod72-design-board.test.tsx` · `align-board.test.tsx` (a)(b)(d).

### 7-2.7 항목 상세 — 다음 단계 카드 · 큰 미리보기 · ⋯ 메뉴 (PR-4)

진단 1·5 "다음 할 일이 흩어져 있고 채운 버튼이 여럿" + 사용자 요청 "비주얼 미리보기가 좀 더 컸으면" → 캔버스 항목 상세. 3.17b의 머리 주 액션 2개(최신본·새 버전 업로드) · '상태 액션' 카드 · 오른쪽 상태·담당·마감 카드 · 컨펌 이력 표 · Phase 4.5 '항목 관리' 카드를 다시 배치한다. **정형 문서(큐시트·시나리오·운영가이드)는 상단 스트립 단일 표시를 유지**하고(큐시트는 다음 PR) 머리의 ⋯ 메뉴만 함께 쓴다.

| 영역 | 규격 |
|---|---|
| 머리 | 경로 `보드 › 카테고리`(보드와 같은 표시 이름) · 제목 + 상태 배지 · 한 줄 메타 `카테고리 · ●담당 · 마감 M월 D일 (요일) + 기한 알약`(승인·확정은 `완료`) · 우측 **[최신본 내려받기](ghost) [⋯]** — 채운 버튼 없음 |
| ⋯ 메뉴 | 36px ghost 버튼(`항목 메뉴`) → 메뉴 232: `고치기` · 구분선 · `지우기`(negative, PM만) + 캡션(권한 범위 또는 종료 사유). 권한 없으면 버튼 자체가 없다. 바깥 누르기·Esc로 닫히고 Esc는 버튼으로 초점을 돌린다. 고치기 = 본문 맨 위 `항목 고치기` 카드, 지우기 = 기존 이름 입력 확인 창 |
| 다음 단계 카드 | 좌측 3px 상태 스트립 · **5단계 레일**(가이드 → 초안 → 내부검토 → 발주처 컨펌 → 확정, 22px 원 · 완료 = accent 원 + 체크 · 현재 = 2px 아웃라인 · 수정요청 = 넷째 칸이 negative `!` + `수정요청` 라벨 + 진입선 negative · 파트너 항목은 첫 칸 `제출 요청`, 주최형·파트너는 넷째 칸 `검토`) + 우측 `5단계 중 n단계( · 되돌아옴)` → 상태 제목 1줄(t-card-title) + 설명 + 버튼(채운 버튼 최대 1개) → PM 내부검토면 아래칸에 컨펌 발송(버전·답 기한) + 반려(사유 필수) |
| 올리기 | 머리에서 이 카드로 옮겼다 — `첫 시안 올리기`·`시안 올리기`·`수정본 올리기`(채운) / `새 버전 올리기`(ghost, 내부검토 요청이 채운 버튼일 때). 누르면 업로드 카드로 초점. **막힌 상태면 버튼이 없고** 설명 아래 캡션 `지금은 새 버전을 올릴 수 없습니다 — 상태 · 이유`(업로드 카드도 그리지 않는다) |
| 큰 미리보기 | 본문 폭 16:9(track 면 · object-contain) — 이미지 파일만 그림, PDF·기타는 확장자 표지 + `이 형식은 화면에서 미리볼 수 없습니다`. 머리 = 버전 토글(2~4개일 때 `v2 최신 · v1`) + 파일 이름 + `새 탭에서 보기` |
| 코멘트 | 머리 칩 `전체 · 내부 메모 · 발주처와 공유`(파트너 항목 = `파트너와 공유`) · 글 = 첫 글자 원 + 이름 + 공개 배지 + 시각, 공유 = steel 틴트 면 · 내부 = canvas 면(옛 좌측 3px 줄 장식은 캔버스 3턴에서 뺐다) · 쓰기 = **공개 범위를 먼저 고르는 토글**(기본 내부 메모) + 안내 한 줄 + `등록`(ghost) |
| 오른쪽 320 | `버전 이력`(96×54 썸네일 · vN + 최신 · **발송본**(열린 컨펌의 버전) · 저장 위치(실서버) · 노트 · 올린 사람·시각 — 누르면 큰 미리보기가 그 버전으로) · `컨펌 기록`(타임라인: vN 발주처에 발송 → 답 기한(지나면 빨강) → 발주처 답 = 의미 배지 + 원문 / 아직 없음) · `제작 가이드`(비면 안내 + PM `채우기` → 고치기 폼) |

- 컨펌대기 설명: `v2를 8월 17일에 보냈고 39일째 답이 없습니다 (기한 8월 21일 — 35일 지남).` — 숫자 뒤 조사는 `objectParticle`(v1을·v2를). PM에게 `발주처 링크 복사`(링크 1개) / `발주처 링크`(여럿·없음 → 행사 설정 ②) — 보드와 같은 규칙(§7-2.6).
- 흰 글자 주황 면 잔존분 정리: 버전 '최신' 알약 → 의미 배지.
- 캔버스 중 구현하지 않은 것: `v1과 나란히 보기`(다음 개선) · `전체 화면`(→ `새 탭에서 보기`로 대신).
- 가드: `src/test/dod73-item-detail.test.tsx` · `align-itemdetail.test.tsx` · `dod66`(잠금) · `dod67`(⋯ 메뉴).

## 8. 실행·완료 기준 (전부 충족 후 체크아웃)

1. 토큰 파일 1곳 정의 → 전 컴포넌트 치환, `grep -r "gray-\|slate-"` 결과 0건
2. 기존 테스트 114/114 유지 + tsc 클린 (기능 무변경 증명)
3. 스크린샷 11장 제출: S0·S1·S2·S3(일반)·S3(큐시트)·S4·S5 체크리스트·S5 간트·S9·발주처 큐(375px)·발주처 현황(375px)
4. 데모 아티팩트 재발행
