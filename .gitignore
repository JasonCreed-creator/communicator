# dependencies
node_modules/

# build output
dist/
dist-demo/
*.tsbuildinfo

# env — secrets never committed (.env.example only)
.env
.env.*
!.env.example
!.env.*.example

# logs / editor
npm-debug.log*
*.log
.DS_Store
.idea/
.vscode/

# demo artifact build output
dist-demo/

# Vercel CLI local build/link output (vercel build 재현용 — 커밋 금지)
.vercel/
